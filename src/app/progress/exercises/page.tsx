"use client";

import Link from "next/link";
import { Suspense, useEffect, useMemo, useState } from "react";
import { useSearchParams } from "next/navigation";
import { createSupabaseBrowserClient } from "@/lib/supabase";

type RawSet={weight:number;reps:number;partial_reps:number;set_type:string;set_number:number;duration_seconds:number|null;notes:string|null};
type ExerciseSession={id:string;exercise_id:string;exercise_name_snapshot:string;workout_session_id:string;notes:string|null;sets:RawSet[]};
type WorkoutSession={id:string;completed_at:string};
type StoredPR={exercise_id:string;exercise_session_id:string;pr_type:"heaviest_weight"|"matched_load_reps";weight:number;reps:number;achieved_at:string};
type Point={exerciseSessionId:string;sessionId:string;date:string;metric:"load"|"time";value:number;weight:number;reps:number;volume:number;weightPR:boolean;repPR:boolean;strengthSignal:number};
type PlateauSignal={detected:boolean;exposures:number;change:number|null};

function parseDurationText(value:string|null|undefined){
  const text=(value??"").trim().toLowerCase();
  if(!text)return null;
  const clock=text.match(/(?:^|\s)(\d{1,3}):(\d{1,2})(?:\s|$)/);
  if(clock){const minutes=Number(clock[1]),seconds=Number(clock[2]);if(seconds<60)return minutes*60+seconds;}
  const seconds=text.match(/(?:^|\s)(\d+(?:\.\d+)?)\s*(?:s|sec|secs|second|seconds)(?:\s|$)/);
  if(seconds)return Math.round(Number(seconds[1]));
  const minutes=text.match(/(?:^|\s)(\d+(?:\.\d+)?)\s*(?:m|min|mins|minute|minutes)(?:\s|$)/);
  if(minutes)return Math.round(Number(minutes[1])*60);
  return null;
}
function bestTimedSeconds(session:ExerciseSession){
  const explicit=(session.sets??[]).filter(s=>s.set_type==="timed"&&Number(s.duration_seconds)>0).map(s=>Number(s.duration_seconds));
  if(explicit.length)return Math.max(...explicit);
  const legacy=[...(session.sets??[]).map(s=>parseDurationText(s.notes)),parseDurationText(session.notes)].filter((v):v is number=>v!==null&&v>0);
  return legacy.length?Math.max(...legacy):null;
}
function validSets(sets:RawSet[]){return sets.filter(s=>["working","top","backoff"].includes(s.set_type)&&s.reps>0);}
function bestWorkingSet(sets:RawSet[]){const v=validSets(sets);if(!v.length)return null;return [...v].sort((a,b)=>Number(b.weight)!==Number(a.weight)?Number(b.weight)-Number(a.weight):b.reps-a.reps)[0];}
function estimatedStrength(w:number,r:number){return w*(1+r/30);}
function path(p:{x:number;y:number}[]){return p.map((x,i)=>`${i===0?"M":"L"} ${x.x} ${x.y}`).join(" ");}
function detectPlateau(points:Point[]):PlateauSignal{if(points.length<4)return{detected:false,exposures:Math.min(points.length,3),change:null};const recent=points.slice(-3),history=points.slice(0,-3);if(recent.some(p=>p.weightPR||p.repPR)||!history.length)return{detected:false,exposures:3,change:null};const prior=Math.max(...history.map(p=>p.strengthSignal)),now=Math.max(...recent.map(p=>p.strengthSignal)),change=prior>0?((now-prior)/prior)*100:null;return{detected:change!==null&&change<1,exposures:3,change};}
function formatImprovement(value:number|null){if(value===null)return"N/A";const sign=value>=0?"+":"";return `${sign}${Math.abs(value)>=1000?Math.round(value).toLocaleString():value.toFixed(1)}%`;}
function formatTime(seconds:number){const m=Math.floor(seconds/60),s=seconds%60;return m>0?`${m}:${String(s).padStart(2,"0")}`:`${s} sec`;}

function ExerciseTrendsContent(){
 const searchParams=useSearchParams();const requestedExercise=searchParams.get("exercise");
 const[exerciseSessions,setExerciseSessions]=useState<ExerciseSession[]>([]),[dates,setDates]=useState<Record<string,string>>({}),[prs,setPrs]=useState<StoredPR[]>([]),[selected,setSelected]=useState(""),[unit,setUnit]=useState<"lb"|"kg">("lb"),[loading,setLoading]=useState(true),[message,setMessage]=useState("");
 useEffect(()=>{const supabase=createSupabaseBrowserClient();async function load(){const{data:{user}}=await supabase.auth.getUser();if(!user){window.location.href="/auth";return;}const[{data:athlete},{data:workouts,error:werr},{data:storedPrs,error:prError}]=await Promise.all([supabase.from("athlete_profiles").select("preferred_unit").eq("user_id",user.id).single(),supabase.from("workout_sessions").select("id,completed_at").eq("athlete_user_id",user.id).eq("status","completed").order("completed_at",{ascending:true}),supabase.from("personal_records").select("exercise_id,exercise_session_id,pr_type,weight,reps,achieved_at").eq("athlete_user_id",user.id).order("achieved_at",{ascending:true})]);setUnit(athlete?.preferred_unit==="kg"?"kg":"lb");if(werr||prError){setMessage(werr?.message??prError?.message??"");setLoading(false);return;}setPrs((storedPrs??[])as StoredPR[]);const rows=(workouts??[])as WorkoutSession[],ids=rows.map(r=>r.id);setDates(Object.fromEntries(rows.map(r=>[r.id,r.completed_at])));if(!ids.length){setLoading(false);return;}const{data,error}=await supabase.from("exercise_sessions").select("id,exercise_id,exercise_name_snapshot,workout_session_id,notes,sets(weight,reps,partial_reps,set_type,set_number,duration_seconds,notes)").in("workout_session_id",ids);if(error){setMessage(error.message);setLoading(false);return;}const ex=(data??[])as ExerciseSession[];setExerciseSessions(ex);const requested=requestedExercise&&ex.some(e=>e.exercise_id===requestedExercise)?requestedExercise:null;if(ex.length)setSelected(requested??ex[0].exercise_id);setLoading(false);}load();},[requestedExercise]);
 const exercises=useMemo(()=>Array.from(new Map(exerciseSessions.map(e=>[e.exercise_id,e.exercise_name_snapshot])).entries()).sort((a,b)=>a[1].localeCompare(b[1])),[exerciseSessions]);
 const selectedPrs=useMemo(()=>prs.filter(p=>p.exercise_id===selected),[prs,selected]);
 const selectedSessions=useMemo(()=>exerciseSessions.filter(e=>e.exercise_id===selected).sort((a,b)=>new Date(dates[a.workout_session_id]).getTime()-new Date(dates[b.workout_session_id]).getTime()),[exerciseSessions,selected,dates]);
 const timedMode=useMemo(()=>selectedSessions.some(e=>bestTimedSeconds(e)!==null),[selectedSessions]);
 const points=useMemo<Point[]>(()=>selectedSessions.map(e=>{const sessionPrs=selectedPrs.filter(p=>p.exercise_session_id===e.id);if(timedMode){const seconds=bestTimedSeconds(e);if(seconds===null)return null;return{exerciseSessionId:e.id,sessionId:e.workout_session_id,date:dates[e.workout_session_id],metric:"time",value:seconds,weight:0,reps:0,volume:0,weightPR:false,repPR:false,strengthSignal:seconds};}const best=bestWorkingSet(e.sets??[]);if(!best)return null;const weight=Number(best.weight);return{exerciseSessionId:e.id,sessionId:e.workout_session_id,date:dates[e.workout_session_id],metric:"load",value:weight,weight,reps:best.reps,volume:weight*best.reps,weightPR:sessionPrs.some(p=>p.pr_type==="heaviest_weight"),repPR:sessionPrs.some(p=>p.pr_type==="matched_load_reps"),strengthSignal:estimatedStrength(weight,best.reps)};}).filter(Boolean)as Point[],[selectedSessions,selectedPrs,timedMode,dates]);
 const values=points.map(p=>p.value),maxV=Math.max(...values,1),minV=Math.min(...values,0),range=Math.max(maxV-minV,1),coords=points.map((p,i)=>({x:points.length===1?50:(i/(points.length-1))*100,y:95-((p.value-minV)/range)*85})),latest=points.at(-1),prior=points.at(-2),gain=latest&&prior?latest.value-prior.value:null,allTimeBest=points.length?[...points].sort((a,b)=>b.strengthSignal-a.strengthSignal)[0]:null,first=points.at(0),overallImprovement=first&&allTimeBest&&first.strengthSignal>0?((allTimeBest.strengthSignal-first.strengthSignal)/first.strengthSignal)*100:null,plateau=detectPlateau(points),weightPRs=selectedPrs.filter(p=>p.pr_type==="heaviest_weight"),repPRs=selectedPrs.filter(p=>p.pr_type==="matched_load_reps");
 if(loading)return<main className="mx-auto min-h-screen max-w-3xl px-6 py-12">Beep boop... locating exercise signals.</main>;
 return<main className="mx-auto flex min-h-screen max-w-3xl flex-col gap-6 px-6 py-10"><header><p className="text-sm font-semibold uppercase tracking-[.25em] text-zinc-400">PHATBOT Exercise Trends</p><h1 className="mt-2 text-3xl font-bold">Exercise telemetry</h1><p className="mt-2 text-zinc-400">{timedMode?"Timed work is tracked duration-to-duration. Legacy time notes such as 2:30 are recognized when possible.":`Track your best non-warmup set, archived milestones, and possible stalls. Loads are shown in ${unit}.`}</p></header>{message&&<p className="rounded-xl border border-zinc-800 p-4">{message}</p>}<label className="flex flex-col gap-2 text-sm font-semibold">Exercise<select value={selected} onChange={e=>setSelected(e.target.value)} className="rounded-xl border border-zinc-700 bg-zinc-950 px-4 py-3">{exercises.map(([id,name])=><option key={id} value={id}>{name}</option>)}</select></label>{points.length===0?<section className="rounded-xl border border-zinc-800 p-5 text-zinc-400">No usable {timedMode?"timed":"working"} sets found for this exercise.</section>:<>{plateau.detected&&<section className="rounded-2xl border border-[#ff0032]/40 bg-[#ff0032]/5 p-5"><p className="text-xs font-bold uppercase tracking-[.2em] text-[#ff0032]">PHATBOT Plateau Signal</p><h2 className="mt-2 text-xl font-bold">This exercise may be hanging around the same performance range.</h2></section>}<section className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><div className="rounded-xl border border-zinc-800 p-5"><p className="text-xs uppercase tracking-widest text-zinc-500">Sessions</p><p className="mt-2 text-3xl font-bold">{points.length}</p></div><div className="rounded-xl border border-zinc-800 p-5"><p className="text-xs uppercase tracking-widest text-zinc-500">All-Time Best</p><p className="mt-2 text-2xl font-bold">{timedMode&&allTimeBest?formatTime(allTimeBest.value):`${allTimeBest?.weight} ${unit} × ${allTimeBest?.reps}`}</p></div><div className="rounded-xl border border-zinc-800 p-5"><p className="text-xs uppercase tracking-widest text-zinc-500">vs Prior</p><p className="mt-2 text-3xl font-bold">{gain===null?"N/A":timedMode?`${gain>=0?"+":""}${gain} sec`:`${gain>=0?"+":""}${gain} ${unit}`}</p></div><div className={`rounded-xl border p-5 ${overallImprovement!==null&&overallImprovement>0?"border-[#ff0032]/50 bg-[#ff0032]/5":"border-zinc-800"}`}><p className="text-xs uppercase tracking-widest text-zinc-500">Overall Improvement</p><p className="mt-2 text-3xl font-black">{formatImprovement(overallImprovement)}</p></div></section>{!timedMode&&selectedPrs.length>0&&<section className="rounded-2xl border border-[#ff0032]/35 p-5"><p className="text-xs font-bold uppercase tracking-[.2em] text-[#ff0032]">🏆 PHATBOT Permanent Milestone Log</p><h2 className="mt-2 text-xl font-bold">Archived PR history</h2><p className="mt-2 text-sm text-zinc-400">{weightPRs.length+repPRs.length} archived lifting milestone{weightPRs.length+repPRs.length===1?"":"s"}.</p></section>}<section className="rounded-2xl border border-[#ff0032]/30 bg-gradient-to-br from-[#ff0032]/5 to-transparent p-5"><p className="text-xs font-bold uppercase tracking-[.2em] text-[#ff0032]">{timedMode?"PHATBOT Duration Signal":"PHATBOT Load Signal"}</p><h2 className="mt-2 text-xl font-bold">{timedMode?"Best duration trend":"Best-set weight trend"}</h2><div className="mt-5 h-56 w-full"><svg viewBox="0 0 100 100" className="h-full w-full overflow-visible" preserveAspectRatio="none"><line x1="0" y1="95" x2="100" y2="95" stroke="currentColor" opacity=".15"/><path d={path(coords)} fill="none" stroke="#ff0032" strokeWidth="2.5" vectorEffect="non-scaling-stroke"/>{coords.map((c,i)=><circle key={i} cx={c.x} cy={c.y} r="2.1" fill="#ffffff" stroke="#ff0032" strokeWidth=".6" vectorEffect="non-scaling-stroke"/>)}</svg></div></section><section className="flex flex-col gap-3">{points.slice().reverse().map(p=><Link key={p.exerciseSessionId} href={`/sessions/${p.sessionId}/report`} className="rounded-xl border border-zinc-800 p-4"><div className="flex items-center justify-between gap-4"><div><p className="font-semibold">{new Date(p.date).toLocaleDateString()}</p><p className="mt-1 text-xs text-zinc-500">Open workout report →</p></div><div className="text-right"><p className="text-xl font-bold">{timedMode?formatTime(p.value):`${p.weight} ${unit} × ${p.reps}`}</p>{!timedMode&&<p className="text-sm text-zinc-400">Set volume {p.volume} {unit}-reps</p>}</div></div></Link>)}</section></>}<div className="grid gap-3 sm:grid-cols-2"><Link href="/progress/workouts" className="rounded-lg border border-zinc-700 px-5 py-3 text-center font-semibold">Workout Trends</Link><Link href="/progress" className="rounded-lg bg-[#ff0032] px-5 py-3 text-center font-semibold text-white">Back to Progress</Link></div></main>;
}
export default function ExerciseTrends(){return<Suspense fallback={<main className="mx-auto min-h-screen max-w-3xl px-6 py-12">Beep boop... locating exercise signals.</main>}><ExerciseTrendsContent/></Suspense>;}
