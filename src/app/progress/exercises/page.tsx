"use client";

import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { createSupabaseBrowserClient } from "@/lib/supabase";

type RawSet={weight:number;reps:number;partial_reps:number;set_type:string;set_number:number};
type ExerciseSession={id:string;exercise_id:string;exercise_name_snapshot:string;workout_session_id:string;sets:RawSet[]};
type WorkoutSession={id:string;completed_at:string};
type Point={sessionId:string;date:string;weight:number;reps:number;volume:number};
function bestWorkingSet(sets:RawSet[]){const valid=sets.filter(s=>s.set_type!=="warmup");if(!valid.length)return null;return [...valid].sort((a,b)=>{if(Number(b.weight)!==Number(a.weight))return Number(b.weight)-Number(a.weight);return b.reps-a.reps;})[0];}
function path(points:{x:number;y:number}[]){return points.map((p,i)=>`${i===0?"M":"L"} ${p.x} ${p.y}`).join(" ");}

export default function ExerciseTrends(){
 const [exerciseSessions,setExerciseSessions]=useState<ExerciseSession[]>([]);const [dates,setDates]=useState<Record<string,string>>({});const [selected,setSelected]=useState("");const [loading,setLoading]=useState(true);const [message,setMessage]=useState("");
 useEffect(()=>{const supabase=createSupabaseBrowserClient();async function load(){const {data:{user}}=await supabase.auth.getUser();if(!user){window.location.href="/auth";return;}const {data:workouts,error:werr}=await supabase.from("workout_sessions").select("id,completed_at").eq("athlete_user_id",user.id).eq("status","completed").order("completed_at",{ascending:true});if(werr){setMessage(werr.message);setLoading(false);return;}const rows=(workouts??[]) as WorkoutSession[];const ids=rows.map(r=>r.id);setDates(Object.fromEntries(rows.map(r=>[r.id,r.completed_at])));if(!ids.length){setLoading(false);return;}const {data,error}=await supabase.from("exercise_sessions").select("id,exercise_id,exercise_name_snapshot,workout_session_id,sets(weight,reps,partial_reps,set_type,set_number)").in("workout_session_id",ids);if(error){setMessage(error.message);setLoading(false);return;}const ex=(data??[]) as ExerciseSession[];setExerciseSessions(ex);if(ex.length)setSelected(ex[0].exercise_id);setLoading(false);}load();},[]);
 const exercises=useMemo(()=>Array.from(new Map(exerciseSessions.map(e=>[e.exercise_id,e.exercise_name_snapshot])).entries()).sort((a,b)=>a[1].localeCompare(b[1])),[exerciseSessions]);
 const points=useMemo<Point[]>(()=>exerciseSessions.filter(e=>e.exercise_id===selected).map(e=>{const best=bestWorkingSet(e.sets??[]);if(!best)return null;return {sessionId:e.workout_session_id,date:dates[e.workout_session_id],weight:Number(best.weight),reps:best.reps,volume:Number(best.weight)*best.reps};}).filter(Boolean).sort((a,b)=>new Date((a as Point).date).getTime()-new Date((b as Point).date).getTime()) as Point[],[exerciseSessions,selected,dates]);
 const maxW=Math.max(...points.map(p=>p.weight),1);const minW=Math.min(...points.map(p=>p.weight),0);const range=Math.max(maxW-minW,1);const coords=points.map((p,i)=>({x:points.length===1?50:(i/(points.length-1))*100,y:95-((p.weight-minW)/range)*85}));const latest=points.at(-1);const prior=points.at(-2);const gain=latest&&prior?latest.weight-prior.weight:null;
 if(loading)return <main className="mx-auto min-h-screen max-w-3xl px-6 py-12">Beep boop... locating exercise signals.</main>;
 return <main className="mx-auto flex min-h-screen max-w-3xl flex-col gap-6 px-6 py-10"><header><p className="text-sm font-semibold uppercase tracking-[.25em] text-zinc-400">PHATBOT Exercise Trends</p><h1 className="mt-2 text-3xl font-bold">Exercise telemetry</h1><p className="mt-2 text-zinc-400">Select an exercise to track your best non-warmup set over time.</p></header>{message&&<p className="rounded-xl border border-zinc-800 p-4">{message}</p>}<label className="flex flex-col gap-2 text-sm font-semibold">Exercise<select value={selected} onChange={e=>setSelected(e.target.value)} className="rounded-xl border border-zinc-700 bg-zinc-950 px-4 py-3">{exercises.map(([id,name])=><option key={id} value={id}>{name}</option>)}</select></label>{points.length===0?<section className="rounded-xl border border-zinc-800 p-5 text-zinc-400">No usable working sets found for this exercise.</section>:<><section className="grid gap-3 sm:grid-cols-3"><div className="rounded-xl border border-zinc-800 p-5"><p className="text-xs uppercase tracking-widest text-zinc-500">Sessions</p><p className="mt-2 text-3xl font-bold">{points.length}</p></div><div className="rounded-xl border border-zinc-800 p-5"><p className="text-xs uppercase tracking-widest text-zinc-500">Latest Best Set</p><p className="mt-2 text-3xl font-bold">{latest?.weight} × {latest?.reps}</p></div><div className="rounded-xl border border-zinc-800 p-5"><p className="text-xs uppercase tracking-widest text-zinc-500">Weight vs Prior</p><p className="mt-2 text-3xl font-bold">{gain===null?"N/A":`${gain>=0?"+":""}${gain}`}</p></div></section><section className="rounded-2xl border border-zinc-800 p-5"><p className="text-xs font-bold uppercase tracking-[.2em] text-zinc-400">🤖 Load Signal</p><h2 className="mt-2 text-xl font-bold">Best-set weight trend</h2><p className="mt-1 text-sm text-zinc-500">Highest non-warmup weight recorded each time you performed this exercise. Reps break ties.</p><div className="mt-5 h-56 w-full"><svg viewBox="0 0 100 100" className="h-full w-full" preserveAspectRatio="none"><line x1="0" y1="95" x2="100" y2="95" stroke="currentColor" opacity=".15"/><path d={path(coords)} fill="none" stroke="currentColor" strokeWidth="2" vectorEffect="non-scaling-stroke"/>{coords.map((c,i)=><circle key={i} cx={c.x} cy={c.y} r="1.8" fill="currentColor"/>)}</svg></div><div className="mt-2 flex justify-between text-xs text-zinc-500"><span>{new Date(points[0].date).toLocaleDateString()}</span><span>{new Date(points.at(-1)!.date).toLocaleDateString()}</span></div></section><section className="flex flex-col gap-3">{points.slice().reverse().map(p=><Link key={p.sessionId} href={`/sessions/${p.sessionId}/report`} className="rounded-xl border border-zinc-800 p-4"><div className="flex items-center justify-between gap-4"><div><p className="font-semibold">{new Date(p.date).toLocaleDateString()}</p><p className="mt-1 text-xs text-zinc-500">Open workout report →</p></div><div className="text-right"><p className="text-xl font-bold">{p.weight} × {p.reps}</p><p className="text-sm text-zinc-400">Set volume {p.volume}</p></div></div></Link>)}</section></>}<div className="grid gap-3 sm:grid-cols-2"><Link href="/progress/workouts" className="rounded-lg border border-zinc-700 px-5 py-3 text-center font-semibold">Workout Trends</Link><Link href="/progress" className="rounded-lg bg-white px-5 py-3 text-center font-semibold text-black">Back to Progress</Link></div></main>;
}