"use client";

import { useEffect } from "react";

function esc(v:string){return v.replace(/[<>&'\"]/g,c=>({"<":"&lt;",">":"&gt;","&":"&amp;","'":"&apos;",'"':"&quot;"}[c]??c));}
const LOGO="/branding/PHATbot%20ICON.png";
async function makeCard(title:string,result:string,subtitle:string){const logo=new Image();logo.src=LOGO;try{await logo.decode()}catch{}const svg=`<svg xmlns="http://www.w3.org/2000/svg" width="1080" height="1350"><defs><radialGradient id="g"><stop stop-color="#ff0032" stop-opacity=".34"/><stop offset="1" stop-color="#050505" stop-opacity="0"/></radialGradient></defs><rect width="1080" height="1350" fill="#050505"/><circle cx="540" cy="520" r="520" fill="url(#g)"/><text x="80" y="115" font-family="Arial Black,Arial" font-size="34" font-weight="900" letter-spacing="8" fill="#ff0032">PHATBOT</text><text x="80" y="178" font-family="Arial,Helvetica" font-size="25" font-weight="700" letter-spacing="5" fill="#777">WIN DETECTED</text><text x="540" y="610" text-anchor="middle" font-family="Arial Black,Arial" font-size="70" font-weight="900" fill="#fff">${esc(title)}</text><text x="540" y="735" text-anchor="middle" font-family="Arial Black,Arial" font-size="76" font-weight="900" fill="#f7c623">${esc(result)}</text><text x="540" y="830" text-anchor="middle" font-family="Arial,Helvetica" font-size="31" font-weight="700" fill="#aaa">${esc(subtitle)}</text><line x1="140" x2="940" y1="1060" y2="1060" stroke="#252525" stroke-width="3"/><text x="540" y="1150" text-anchor="middle" font-family="Arial Black,Arial" font-size="30" font-weight="900" fill="#fff">TRAIN. TRACK. IMPROVE.</text><text x="540" y="1225" text-anchor="middle" font-family="Arial,Helvetica" font-size="24" font-weight="700" fill="#777">Powered by PHATBOT</text></svg>`;const image=new Image(),url=URL.createObjectURL(new Blob([svg],{type:"image/svg+xml;charset=utf-8"}));try{await new Promise<void>((res,rej)=>{image.onload=()=>res();image.onerror=rej;image.src=url});const c=document.createElement("canvas");c.width=1080;c.height=1350;const x=c.getContext("2d");if(!x)throw new Error("Canvas unavailable");x.drawImage(image,0,0);if(logo.complete&&logo.naturalWidth)x.drawImage(logo,440,245,200,200);return await new Promise<Blob>((res,rej)=>c.toBlob(b=>b?res(b):rej(new Error("PNG generation failed")),"image/png",1))}finally{URL.revokeObjectURL(url)}}
async function shareCard(title:string,result:string,subtitle:string){const blob=await makeCard(title,result,subtitle),file=new File([blob],"phatbot-win.png",{type:"image/png"});if(navigator.share&&navigator.canShare?.({files:[file]})){await navigator.share({title:`PHATBOT ${title}`,text:`${title}: ${result}`,files:[file]});return}saveBlob(blob,"phatbot-win.png")}
function saveBlob(blob:Blob,name:string){const href=URL.createObjectURL(blob),a=document.createElement("a");a.href=href;a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(href),1000)}
function openMenu(title:string,result:string,subtitle:string){document.querySelector("[data-phatbot-share-menu]")?.remove();const wrap=document.createElement("div");wrap.dataset.phatbotShareMenu="true";wrap.className="fixed inset-0 z-[100] flex items-end bg-black/60 p-4 backdrop-blur-sm";wrap.innerHTML=`<div class="mx-auto w-full max-w-md rounded-3xl border border-zinc-800 bg-[#090909] p-5 shadow-2xl"><div class="mb-4"><p class="text-[10px] font-black uppercase tracking-[.2em] text-[#ff0032]">PHATBOT SHARE</p><p class="mt-1 text-lg font-black text-white">${esc(title)}</p><p class="mt-1 text-xs text-zinc-500">${esc(result)}</p></div><div class="grid gap-2"><button data-action="share" class="rounded-xl bg-[#ff0032] px-4 py-3 text-sm font-black text-white">SHARE IMAGE</button><button data-action="save" class="rounded-xl border border-zinc-700 px-4 py-3 text-sm font-black text-white">SAVE IMAGE</button><button data-action="cancel" class="px-4 py-3 text-xs font-black uppercase tracking-wider text-zinc-500">Cancel</button></div></div>`;wrap.addEventListener("click",async e=>{const action=(e.target as HTMLElement).closest("button")?.getAttribute("data-action");if(!action){if(e.target===wrap)wrap.remove();return}if(action==="cancel"){wrap.remove();return}try{if(action==="share")await shareCard(title,result,subtitle);else{const b=await makeCard(title,result,subtitle);saveBlob(b,"phatbot-win.png")}}finally{wrap.remove()}});document.body.appendChild(wrap)}
function enableLongPress(target:HTMLElement,title:string,result:string,subtitle:string){if(target.dataset.phatbotLongpress)return;target.dataset.phatbotLongpress="true";target.style.touchAction="pan-y";let timer:number|undefined,moved=false;const clear=()=>{if(timer)window.clearTimeout(timer);timer=undefined};target.addEventListener("pointerdown",()=>{moved=false;clear();timer=window.setTimeout(()=>{if(!moved)openMenu(title,result,subtitle)},550)});target.addEventListener("pointermove",()=>{moved=true;clear()});target.addEventListener("pointerup",clear);target.addEventListener("pointercancel",clear)}
export default function ReportShareActions(){useEffect(()=>{const attach=()=>{const main=document.querySelector("main");if(!main)return;main.querySelectorAll("[data-phatbot-share]").forEach(n=>n.remove());const workout=main.querySelector("h1")?.textContent?.trim()??"Workout",sections=[...main.querySelectorAll<HTMLElement>("section")];const win=sections.find(s=>(s.textContent??"").includes("PHATBOT WIN CARD"));if(win){const title=win.querySelector("h2")?.textContent?.trim()??"Personal Record",result=[...win.querySelectorAll("p")].map(p=>p.textContent?.trim()??"").find(v=>/\d+\s*(lb|kg)\s*[×x]\s*\d+/i.test(v))??"New PR";enableLongPress(win,title,result,workout)}const prs=sections.find(s=>/Personal Records/i.test(s.textContent??""));if(prs)for(const card of [...prs.querySelectorAll<HTMLElement>("div.rounded-xl")]){const ps=[...card.querySelectorAll("p")].map(p=>p.textContent?.trim()??"").filter(Boolean);if(ps.length>=2)enableLongPress(card,ps[0],ps[1],workout)}for(const sec of sections){const t=sec.textContent??"";if(t.includes("Progressive Overload Score")){const m=t.match(/Progressive Overload Score\s*(BASELINE|\d+%)/i)?.[1];if(m&&m!=="BASELINE"&&parseFloat(m)>50)enableLongPress(sec,"Progressive Overload Win",m,workout)}if(t.includes("Training Volume vs Last Workout")){const m=t.match(/Training Volume vs Last Workout\s*(N\/A|[+-]?\d+(?:\.\d+)?%)/i)?.[1];if(m&&m!=="N/A"&&parseFloat(m)>0)enableLongPress(sec,"Training Volume Win",m,workout)}}};attach();const o=new MutationObserver(attach);o.observe(document.body,{childList:true,subtree:true});return()=>o.disconnect()},[]);return null;}
