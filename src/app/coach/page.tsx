"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { createSupabaseBrowserClient } from "@/lib/supabase";

type ClientRow={athleteId:string;name:string;latestWorkout:string|null;latestWorkoutAt:string|null;latestWorkoutId:string|null;workoutsThisWeek:number;needsFeedback:boolean;feedbackLabel:string|null};
function mondayStart(){const date=new Date();const day=date.getDay();date.setDate(date.getDate()-(day===0?6:day-1));date.setHours(0,0,0,0);return date;}
export default function CoachDashboardPage(){
 const [loading,setLoading]=useState(true);const [enabled,setEnabled]=useState(false);const [clients,setClients]=useState<ClientRow[]>([]);const [message,setMessage]=useState("");
 useEffect(()=>{const supabase=createSupabaseBrowserClient();async function load(){const {data:{user}}=await supabase.auth.getUser();if(!user){window.location.href="/auth";return;}const {data:coach}=await supabase.from("coach_profiles").select("user_id,business_name").eq("user_id",user.id).maybeSingle();if(!coach){setEnabled(false);setLoading(false);return;}setEnabled(true);const {data:links,error}=await supabase.from("coach_athletes").select("athlete_user_id").eq("coach_user_id",user.id).eq("active",true);if(error){setMessage(error.message);setLoading(false);return;}const weekStart=mondayStart();const rows:ClientRow[]=[];for(const link of links??[]){const athleteId=link.athlete_user_id;const [profileResult,latestResult,weeklyResult]=await Promise.all([supabase.from("profiles").select("display_name").eq("id",athleteId).maybeSingle(),supabase.from("workout_sessions").select("id,workout_name_snapshot,completed_at").eq("athlete_user_id",athleteId).eq("status","completed").order("completed_at",{ascending:false}).limit(1).maybeSingle(),supabase.from("workout_sessions").select("id",{count:"exact",head:true}).eq("athlete_user_id",athleteId).eq("status","completed").gte("completed_at",weekStart.toISOString())]);const latest=latestResult.data;let needsFeedback=false,feedbackLabel:string|null=null;if(latest?.id){const {data:feedback}=await supabase.from("coach_workout_feedback").select("id,updated_at").eq("workout_session_id",latest.id).eq("coach_user_id",user.id).order("updated_at",{ascending:false}).limit(1).maybeSingle();needsFeedback=!feedback;if(needsFeedback)feedbackLabel="New workout awaiting feedback";else feedbackLabel="Latest workout reviewed";}rows.push({athleteId,name:profileResult.data?.display_name??"Athlete",latestWorkout:latest?.workout_name_snapshot??null,latestWorkoutAt:latest?.completed_at??null,latestWorkoutId:latest?.id??null,workoutsThisWeek:weeklyResult.count??0,needsFeedback,feedbackLabel});}rows.sort((a,b)=>Number(b.needsFeedback)-Number(a.needsFeedback)||(new Date(b.latestWorkoutAt??0).getTime()-new Date(a.latestWorkoutAt??0).getTime()));setClients(rows);setLoading(false);}load();},[]);
 if(loading)return <main className="mx-auto min-h-screen max-w-3xl px-6 py-12 text-zinc-300">Beep boop... scanning client activity.</main>;
 if(!enabled)return <main className="mx-auto flex min-h-screen max-w-2xl flex-col gap-6 px-6 py-10"><header><p className="text-sm font-semibold uppercase tracking-[.25em] text-zinc-400">PHATBOT Coach</p><h1 className="mt-2 text-3xl font-bold">Coach Dashboard</h1></header><section className="rounded-xl border border-zinc-800 p-6"><h2 className="text-xl font-semibold">Coach mode is not enabled yet.</h2><p className="mt-2 text-zinc-400">Enable coach mode from your Account page first. Your athlete account and workout history will stay intact.</p><Link href="/account" className="mt-5 inline-block rounded-lg bg-white px-5 py-3 font-semibold text-black">Go to Account</Link></section></main>;
 const attention=clients.filter(c=>c.needsFeedback).length;
 return <main className="mx-auto flex min-h-screen max-w-3xl flex-col gap-6 px-6 py-10"><header className="flex items-start justify-between gap-4"><div><p className="text-sm font-semibold uppercase tracking-[.25em] text-zinc-400">PHATBOT Coach</p><h1 className="mt-2 text-3xl font-bold">Coach Dashboard</h1><p className="mt-2 text-zinc-400">Beep boop. Client activity and coaching opportunities, prioritized.</p></div><div className="flex gap-2"><Link href="/coach/invitations" className="rounded-lg border border-zinc-700 px-4 py-2 text-sm font-semibold">Invitations</Link><Link href="/" className="rounded-lg border border-zinc-700 px-4 py-2 text-sm font-semibold">Athlete View</Link></div></header>{message&&<p className="rounded-xl border border-zinc-800 p-4 text-sm">{message}</p>}<section className="grid gap-3 sm:grid-cols-2"><div className="rounded-xl border border-zinc-800 p-5"><p className="text-sm text-zinc-500">Active Clients</p><p className="mt-2 text-4xl font-black">{clients.length}</p></div><div className="rounded-xl border border-zinc-600 p-5"><p className="text-sm text-zinc-500">🤖 Feedback Opportunities</p><p className="mt-2 text-4xl font-black">{attention}</p><p className="mt-1 text-xs text-zinc-500">Latest workouts without your feedback</p></div></section>{attention>0&&<section className="rounded-2xl border border-zinc-600 p-5"><p className="text-xs font-bold uppercase tracking-[.2em] text-zinc-400">🤖 PHATBOT Coach Signal</p><h2 className="mt-2 text-xl font-bold">{attention} client{attention===1?" has":"s have"} a fresh workout ready for review.</h2><p className="mt-2 text-sm text-zinc-400">Clients needing feedback are moved to the top of your roster so the next coaching action is obvious.</p></section>}<section className="flex flex-col gap-3">{clients.length===0?<div className="rounded-xl border border-zinc-800 p-6"><h2 className="text-lg font-semibold">No clients linked yet</h2><p className="mt-2 text-sm text-zinc-400">Accepted athlete invitations will appear here.</p></div>:clients.map(client=><Link href={`/coach/athletes/${client.athleteId}`} key={client.athleteId} className={`rounded-xl border p-5 transition hover:border-zinc-500 ${client.needsFeedback?"border-zinc-500":"border-zinc-800"}`}><div className="flex items-start justify-between gap-4"><div><div className="flex flex-wrap items-center gap-2"><h2 className="text-lg font-semibold">{client.name}</h2>{client.needsFeedback&&<span className="rounded-full border border-zinc-500 px-2 py-1 text-[10px] font-bold uppercase tracking-wider">🤖 Review</span>}</div><p className="mt-1 text-sm text-zinc-500">{client.workoutsThisWeek} workout{client.workoutsThisWeek===1?"":"s"} this week</p></div><span className="rounded-full border border-zinc-700 px-3 py-1 text-xs font-bold uppercase">View Athlete</span></div><div className="mt-4 rounded-lg bg-zinc-900 p-4"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-wider text-zinc-500">Latest Workout</p><p className="mt-1 font-semibold">{client.latestWorkout??"No completed workouts"}</p>{client.latestWorkoutAt&&<p className="mt-1 text-xs text-zinc-500">{new Date(client.latestWorkoutAt).toLocaleString()}</p>}</div>{client.feedbackLabel&&<p className={`text-right text-xs font-semibold ${client.needsFeedback?"text-zinc-200":"text-zinc-500"}`}>{client.feedbackLabel}</p>}</div></div></Link>)}</section></main>;
}