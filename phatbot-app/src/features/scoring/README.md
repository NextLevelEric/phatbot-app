# PHATBOT Scoring Domain

Progressive-overload calculations, workout scoring, weekly scoring, and PR detection live here as deterministic TypeScript domain logic. UI components should consume results from this layer rather than duplicate calculation logic.
